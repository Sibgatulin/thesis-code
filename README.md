# Importable definitions for my thesis

This repo is a fresh start, where I move the functions that have proved to be useful from my old repo. 

In general, I wanted to use `qpmri` where possible, but I somehow lost trust in its multilevel architecture, because it now yields posterior samples with inter-voxel correlation approx. 1
